module TypeDeclaration where

-- Prelude> let fifteenInt = fifteen :: Int
-- Prelude> let fifteenDouble = fifteen :: Double

-- Prelude> let f1 :: Num a => a -> a -> a; f1 x y = x + y
-- Prelude> let f2 :: (Num a, Num b) => a -> b -> b; f2 x y = undefined

fifteenInt = fifteen :: Int
fifteenDouble = fifteen :: Double